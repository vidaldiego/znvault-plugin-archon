/**
 * CLI-side entry point: registers the `znvault archon …` command group
 * with the znvault CLI plugin host.
 */
export function createArchonCLIPlugin(): never {
  throw new Error('not implemented');
}
