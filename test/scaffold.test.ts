import { describe, expect, it } from 'vitest';

// Placeholder scaffold test — real coverage lands with the differ,
// migration runner, and route/CLI tasks later in the plan.
describe('scaffold', () => {
  it('has a passing placeholder test', () => {
    expect(true).toBe(true);
  });
});
